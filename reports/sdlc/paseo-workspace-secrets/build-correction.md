# Corrected build
Independent finding reproduced with existing negations for both files. Setup now checks the final ignore line and appends a wildcard if necessary. Existing file preservation unchanged. Matching copies applied to both repositories. Correction Red, Green, post-refactor and installed runtime smoke retained separately.

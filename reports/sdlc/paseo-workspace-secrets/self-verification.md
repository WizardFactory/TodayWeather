# Self-verification
AC1 and AC2 pass synthetic and installed-runtime checks. AC3 passes installed schema parse and identical config comparison. Installed seedPaseoConfigFile and runWorktreeSetupCommands exercised in an actual detached Git worktree using source secrets, compared without printing. This is real implementation execution, not a newly registered Paseo workspace through daemon RPC. All temporary worktrees removed. Both existing checkouts initialized, Git diff whitespace check and exclusion checks pass. Existing base credentials permissions are preserved; newly copied credentials use 0600. No AWS/SSH or app startup.

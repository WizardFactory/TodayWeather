# Change record
Added paseo.json in both checkouts; appended credentials ignore rule and workspace instructions in both EC2 guides. Created local .aws/.gitignore, copied credentials into current worktree only where absent. Existing source secret contents and existing destination files preserved. No commit or remote action.

# Assessment of comment 5819062333

Source: https://github.com/WizardFactory/TodayWeather/pull/2562#issuecomment-5819062333 (updated 2026-09-24 17:37:30 UTC). Reviewed against unchanged local/remote head `84bb1af88eefa5e152da61142d9999443c0d1bfe`; remote web/RSS checks passed. This assesses existing feedback, not a new independent PR review or approval.

## Findings and disposition

| Item | Assessment | Evidence / location | Recommended follow-up |
| --- | --- | --- | --- |
| 1. Hourly uses only three-hour KMA data | Confirmed; medium-priority functional recommendation | `packages/weather-core/src/index.ts:391` chooses `raw.short`, ignoring `raw.shortest`. The captured response has 41 short rows and five shortest rows at 03–07 on September 25, each with t1h and rn1=-1. The UI filters future rows correctly, but cannot show omitted one-hour temperatures. | Merge source-aware shortest points into near-term forecast, prefer their valid temperature at duplicate timestamps, preserve later short points and source-specific 1h/3h accumulation metadata. Missing rn1 remains null, never zero. If deferred, disclose actual 3-hour sampling rather than imply uniformly hourly values. Test overlap, midnight, sentinels, units and missing fields. |
| 2. City air view discards summary | Confirmed, with a freshness caveat; medium-priority recommendation | `normalizeWeather` emits zero air stations and drops `current.summaryAir` despite the response containing “대기상태가 좋아요”. `web/src/Weather.tsx:507` therefore renders the empty air view. Legacy mobile controller.forecastctrl.js preserves summaryAir. | Retain a separate provider-summary string and show it as supplemental text when numeric observations are missing, without inventing station values or changing numeric availability. If air observation time is unknown, disclose that; do not present “good” as a verified fresh measurement. Investigate missing upstream measurements separately. |
| 3. Current location shows English road name | Conditional; not reproduced with the current client request | `web/src/direct-api.ts:94` already sends Accept-Language: ko. Captured ko request returns name=명동 and current geo() returns 명동. Separate requests without language or with en return Taepyeongno 1(il)-ga, with Korean kmaAddress in both. `server/controllers/geo.controller.js:522` consumes the language header. | Preserve ko. Defensive KR fallback/preference using nonempty kmaAddress.name3/name2/name1 is reasonable if upstream/cache varies, but describe this as resilience rather than proof the present default path is broken. Avoid replacing catalog/custom user names globally. Test KR with/without Korean components and foreign locations. |
| 4. Overseas catalog calls fail | Confirmed upstream availability issue; client body suppression should remain | Catalog Tokyo and London each return HTTP 501 text/plain with no Access-Control-Allow-Origin. These server responses precede normalization. Current readJson rejects non-JSON before reading/displaying its body; direct browsers may instead see a CORS fetch failure. | Investigate the existing world-weather API/provider route separately; source response alone cannot establish root cause. Preserve generic user errors, retry and no synthetic fallback. If publicly launching before recovery, explicitly indicate overseas availability limits rather than advertise currently working coverage. |

## Critical distinctions

- The nationwide response contains numeric pollutants, but its first Seoul observation is **2021-05-25**. It is neither a valid replacement for today's city observation nor evidence that domestic collection is currently healthy.
- The city summary has no independently established air observation time in this payload. Its presence does not prove fresh pollutant measurements; upstream summary generation, response shaping and collector state require a separate investigation. No production SSH/database/collector action was run here.
- Missing-hourly data is a confirmed selection gap; `rn1=-1` already passes through negative-value rejection in the shared point adapter when used. Any merge must preserve differing accumulation periods and must not manufacture missing metrics.
- The geocode language result is an observation at this time, not a universal guarantee for every coordinate. Reviewer and current code request conditions differ in a way that reproduces the name difference.
- The reviewer classified all four as recommendations and none as mandatory. This assessment does not invent a new merge blocker, certify production readiness, or close the open mobile-parity issue. Prioritize 1 and 2 for product quality; 3 is defensive hardening; 4 needs existing backend follow-up.

## Verification

Executed seven bounded read-only public requests: Seoul weather, ko reverse geocode, nationwide air, Tokyo, London, plus no-language/en reverse geocode. Source-shaped response fields and normalization results are recorded in probes.json and geocode-language.json. Internal error bodies are deliberately omitted; safe parser messages were tested with captured non-JSON replies. No new runtime test suite was needed for this source/comment assessment. No application edits, commit, push, remote comment, cloud mutation or deployment occurred.

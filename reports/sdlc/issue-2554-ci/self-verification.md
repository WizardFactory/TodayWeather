# Self verification

PASS: intended missing-workflow red; green and final contract checks including command failure propagation; additional exact-YAML local execution with fresh npm packages passed 43 regression and 36 full-router cases. Independent verifier separately passed and covered dependency-install failure too. Workflow and README digests match candidate.json and independent report. README link resolves. Git diff whitespace checks passed.

This is local verification, not hosted checkout/setup or a cross-provider PR review. Smoke weather/model inputs are synthetic. Initial automatic permission review timed out; a permitted retry succeeded after creating the missing local runner script. No production operations occurred. Transitive npm dependencies remain registry-resolved, matching existing smoke instructions. Hosted CI observation follows authorized push.

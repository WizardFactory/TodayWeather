# Second review correction plan

Continue stable issue-2560 task on b13dc38a; user authorized correction, with prior PR push authorization retained. No merge, deployment, provider or production DB access.

A: share weather acceptance/conversion mapping and include both shower labels (legacy rain-compatible pty1; retain original Korean text). B: keep hourly41slot contract; preserve raw short source separately for daily targets beyond D+2, validate each source publication (0–24h, target <=KST pubdate+4), select latest per slot, require explicit usable extrema/weather, omit unadjusted rain/snow totals. DB2 uses each document publication. DB1 stores the current raw batch in optional dailySource, replacing it completely on each save; legacy documents without it remain honestly unavailable. Coverage health flags invalid rows and missing dates from today through last available future date; unsupported trailing dates remain listed separately. C: retire scheduled midrss, keep short RSS. D: missing humidity alone cannot discard valid RSS daily weather; retain missing-extrema policy; remove redundant global tempString.

Use new isolated daily-review tests (shower parser/storage/conversion, health, scheduler, source validity/snapshot replacement) before code changes. Extend full v000903 smoke for D+3 present/absent/stale/partial, both DB formats/C/F, shower weather and missing RSS humidity. Model/HTTP/timers/config stubbed before module evaluation; never execute app startup. Preserve recent history, source publications, hourly shape, unknown rain totals and old consumer field selection. Test source-level clients; native builds remain not run. Record real red/green/regression plus additional route smoke and independent verifier findings. Update architecture via Archify. Keep new repository evidence concise; bulk historical evidence cleanup is deferred because earlier receipts reference those exact files.

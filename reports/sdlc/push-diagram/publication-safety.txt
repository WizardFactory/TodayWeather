Publication scan on 2026-09-23: 58 then-current changed/untracked files scanned against local AWS credential values without printing them and AWS/private-key patterns; zero matches. .aws/credentials and .aws/aleckim.pem remain gitignored. Exported aggregate evidence has no raw client-IP, cookie, query, device-token or coordinate fields. Existing source artifact and browser hashes remain unchanged.
Staged whitespace check: plain git diff --cached --check flags standard CSV CRLF and blank context lines in preserved historical diff snapshots. With core.whitespace=cr-at-eol and change-record*.txt excluded, the staged diff check passes. Those evidence/data bytes are intentionally preserved.

# Investigation
Reviewed entrypoints, current/legacy routers, Manager timers and persistence selection, KMA request/merge paths, DSF and AQI cache fills, Angular request and response code, shared storage, native widget constants, Gulp and historical Travis recipes.
Primary findings are preserved in docs/architecture/service-overview.md, weather-collection.md, mobile-api.md and evidence.md, with file/function references.
Confirmed unknowns: public /weather and /geocode gateway implementation; actual release config; live topology/provider availability.
No application runtime, collectors or external APIs executed. No product source modified.
The client retries on slow response via 2-second timers, not conventional failure retry. Domestic scheduling uses UTC minutes and mixed queued/direct jobs. Latest DSF path is request-time; legacy recurring collector startup is absent.
Skills: canonical paths resolved through installed Codex skill links. sdlc, planning-with-files and archify contracts read. Archify 2.17 update checker returned silent/current. No skills installed or updated.

## AWS correlation continuation
AK authorized read-only AWS CLI access to account 141248341265. Verified STS, queried CloudFront/DNS/REST API deployed stage/Lambda metadata and selected nonsecret settings/EventBridge/EC2 metadata/CloudWatch. Downloaded five deployment ZIPs privately and matched AWS CodeSha256; inspected business logic without executing bundles. Credentials unchanged; no function invocation, SSH, provider probe or cloud mutation.
Confirmed: weather/geocode -> CloudFront -> API Gateway production -> Lambda. Weather Lambda geocodes using DynamoDB and provider adapters, forwards to SERVICE_SERVER=http://tw-svc-spot.wizardfactory.net, whose DNS matches service EC2. KR goes to versioned KMA address; non-KR to DSF coordinates. Unversioned requests default v000901. Weather address handler returns 501. Geocode cache 30 days; weather success max-age=300. KAQ copier producer and repository S3 consumer share object naming; deployed consumer bucket/mode unverified. EventBridge ends hour23, repository schedule hour13. Latest queried 24h: weather 5169 calls/0 Lambda errors, copier24/24. Bounded logs show missing OCR description TypeError in _getDate; empty OCR root cause not established.
Sanitized control-plane evidence, exact observation/metric times, deployment hashes and selected nonsecret excerpts are under docs/architecture. Original full report is an input, not a substitute for fresh evidence. EC2 process/code/database version and client release configuration remain unknown.

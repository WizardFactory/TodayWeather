# QA-1 assignment

Role: qa. Stage independent-verification, iteration 1/10 (9 remaining). Builder context `/root`, provider OpenAI. Host exposed `collaboration.spawn_agent` provides a fresh OpenAI context with inherited model, shared filesystem and existing managed permissions, four total slots, messages/result notification and interrupt/follow-up. This is not alternate-provider PR review. Scope authorization: user requested issue publication and implementation; SDLC skill mandates independent verification for local implementation. No further delegation.

Candidate: `candidate.json`, base `87b8855f308611a07897cd3a39c45fefb3088d77`, digest `sha256:48ebadaa40e872234a599e41d2ec3c749374eae72a0a1e8ca81a2bd654c872a0`.

Read root AGENTS.md, installed SDLC verification instructions, reports/sdlc/webapp-implementation/{artifacts,spec,test-plan,build,self-verification,candidate,test-results}, docs/webapp/{specification,implementation}, and relevant actual changed files. Main has frozen implementation files for assessment. Independently challenge correctness, data semantics, persistence/offline, auth/security, execution/deployment claims, and gaps against the maximum mobile parity objective. Distinguish declared release gates from unintended implementation defects; do not call full parity done.

Read-only source; output only reports/sdlc/webapp-implementation/qa-1.md, qa-1.json and qa-1-*.log or /tmp scratch. Run isolated checks as useful; no legacy server/provider writes, real push, git mutation, remote publication, deployment, installs, unrelated edits, shared-state writes or subdelegation. npm unit tests need listener permission; use existing managed escalation only if needed. Browser may be driven independently with installed Playwright and /root/.cache/ms-playwright/chromium-1217/chrome-linux64/chrome, against an isolated demo process on a free port. Do not overwrite main screenshots/reports or use port 4175 (main's live smoke process).

Deliver within a bounded review (approximately 10 minutes): observed commands/exit/results, consumed inputs/digests, source file-set agreement, findings with severity/location/reproduction/requirement/confidence/disposition, actual context/provider/model when available, limitations and proposed HOTL decision. Do not mutate fixes. Send actionable findings early. Main alone owns integration and shared state.

# Executable implementation contract
Upstream: docs/webapp/specification.md and technical-design.md. Build a unified Korean-first responsive web surface with an explicit sample-data mode, a live same-origin BFF, public place catalog/search, typed raw KMA/world adapters, stable favorite identity and value/unit provenance.
Use app routes for hourly/daily/air/combined, locations, national weather/air, warnings, settings, notifications and help. Weather API returns normalized schema; sample mode is opt-in and conspicuously labeled. Default live mode fails closed when upstream unavailable. Browser starts on manual city selection unless state/public link resolves one; geolocation only on click.
Implement notification capability API/UI and owned persistence/scheduling where configuration permits; do not claim real-device delivery without observed evidence. Billing and native widget/Watch remain explicitly unavailable web capabilities pending product policy.
PWA assets cache independently of weather snapshots; network API cannot fall back to fabricated data. Deep-link and previous-worker recovery must be tested. Prefer conservative canonical upstream units with local verified conversions if live legacy unit correctness is unresolved. All six settings remain represented; standards without source evidence stay unavailable instead of relabeled.
Release prerequisites: provider/source freshness, external search licensing/quota, actual notification device delivery, production domain/budget/operations approval. Local completion must enumerate exact remaining gates.

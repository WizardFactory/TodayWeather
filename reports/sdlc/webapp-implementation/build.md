# Build assessment — correction iteration 2

Implemented the isolated root web workspace, typed KMA/DSF data adapters, same-origin Node BFF, responsive read/settings/location screens, versioned preferences and snapshots, install/offline worker, optional owned generic reminder service, CI recipe and a single-instance HTTPS staging recipe. The exact relevant changed source/test/config/docs file set and SHA-256 values are in `candidate.json`; `changes.patch` preserves the real tracked/untracked diff. Existing native client/server sources are unchanged.

The local implementation contract is fulfilled as an executable integration candidate. Full public parity remains open in issue #2558: conditional weather alerts, real installed iOS/Android push behavior, upstream freshness/air availability, international provider verification, native purchase policy and production infrastructure. The reminder service is explicitly single process and disabled by default. It sends generic reminders, not actual forecast/condition messages. The cloud proposal remains distinct from the implemented Node runtime.

Validation: strict TypeScript and production build passed; 27 unit/API tests and eight real Chromium scenarios passed. Public API reads plus an actual live browser read passed with missing/stale data visibly represented. Docker/Compose and remote CI were authored but not executed. The architecture HTML passed 9/9 artifact checks and automated browser checks; main inspected light 1440x900 and dark 2048x1320 captures, with readable routes/cards and no occlusion.

TDD evidence: initial domain assertions failed against deliberate stubs (8 failing, 1 passing); domain implementation passed nine tests. Initial API test setup had an incorrect import, then sandbox listener restrictions; neither counts as an intended behavior red/pass. These failure logs are retained. Later tests added regressions for observed legacy sentinel/stale-date behavior. Early browser locator errors were corrected to use actual combobox accessible names; application browser cases passed afterward.

Correction evidence: `corrections.md`. QA1-F1/F2/F3 plus main N1 were addressed. The new nation regression and four actual-browser regressions fail against the prior implementation; all pass on the current candidate. Original iteration evidence is retained in `iteration-1/` and QA-1 outputs.

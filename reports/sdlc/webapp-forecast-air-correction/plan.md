# Plan

Add failing core + snapshot regressions, implement pure merge/summary shaping and reusable summary UI, add raw KMA browser scenario with hourly intervals and offline summary, update docs/diagram, verify and freeze candidate for independent QA, commit/push existing branch.

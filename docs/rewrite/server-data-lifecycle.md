# Server data lifecycle and rewrite dependencies

Source baseline: `bd6640f2` (re-baselined 2026-09-25 from `ff7acf3996ccb66c912d2ed4710cf300197d6966`, inspected 2026-09-23; client source identical). Upstream collection changes between the two (KMA endpoint migration, mid forecast policy, mid RSS retirement, short `dailySource` snapshots, opt-in ASOS history recovery) are marked with their commit. This document follows acquisition → persistence → read-time composition. Provider availability, credentials, current data freshness and present deployment were not tested, and the upstream offline tests were not run for this document. The [response assembly reference](server-response-assembly.md) contains the exact consumer order; [existing collection architecture](../architecture/weather-collection.md) supplies additional historical context, and [gather source reconciliation](../architecture/gather-source-reconciliation.md) records the `apis.data.go.kr` migration and its inactive EC2 policy snapshot.

## Product and storage map

Paths below identify checked-in models/adapters, not an inventory from a live MongoDB. Collection names derive from Mongoose models and can be pluralized by its configuration; do not migrate by guessing a collection name from a filename. Derived collection names, identity keys, indexes and retention: [data model reference](data-model-reference.md).

| Product | Producer / acquisition | Model or adapter | Read-time role and important key |
| --- | --- | --- | --- |
| Town/address/grid metadata | Town/geocode maintenance and local metadata. **Request-time creation:** when a domestic address misses the cached list, `_findTown` geocodes it through Kakao (`KAKAO_SECRET_KEYS`) and then Google in `ko` (`GOOGLE_SECRET_KEY`), converts it to the grid, and inherits `areaNo` from a same-grid or same-`first/second` town. An in-Korea result is inserted into `towns` without awaiting the save, in any mode including `service` ([rules](server-response-assembly.md#location-resolution-ordered)) | [town](../../server/models/town.js), [area numbers](../../server/models/modelAreaNo.js), [forecast zones](../../server/controllers/kma/kma.forecast.zone.controller.js), [town lookup](../../server/controllers/controllerTown.js#L4245-L4466), [geocoders](../../server/utils/convertGeocode.js) | Address → `{mx,my}` and `{lat,lon}`; life/health area and medium forecast zone selection. Each KMA controller instance loads the whole collection once, on first request, and never refreshes it; other controller instances and processes see new towns only after restart. Failed geocoding is not cached and is repeated by later stages ([failure row](#freshness-fallback-and-failure-boundaries)) |
| Grid current observation | Manager `/gather/current` → KMA requester | [legacy current](../../server/models/modelCurrent.js), [v2 current model](../../server/models/kma/kma.town.current.model.js), [v2 adapter](../../server/controllers/kma/kma.town.current.controller.js) | Current/history; grid + valid time; observation priority and yesterday |
| Short forecast | Manager `/gather/short` (`getVilageFcst` on `apis.data.go.kr` since `5e653285`) | [legacy short](../../server/models/modelShort.js), [v2 short adapter](../../server/controllers/kma/kma.town.short.controller.js). DB 1.0 documents also hold `dailySource {pubDate, rows}`, the raw daily fields of the last collected batch, replaced on every successful [`saveShort`](../../server/controllers/controllerManager.js#L235-L237) (`49afbbea`; older documents lack it until the next save) | Three-hour forecast baseline; grid, forecast time, publication. Raw snapshots (DB 2.0: every stored slot with its document `pubDate`) supply daily rows after D+2 when 0–24 h old ([item 20](server-response-assembly.md#3-domestic-merge-rules-in-execution-order)) |
| Shortest forecast | Manager `/gather/shortest` | [legacy shortest](../../server/models/modelShortest.js), [v2 shortest adapter](../../server/controllers/kma/kma.town.shortest.controller.js) | Current fallback, sky and near-term/rain corrections |
| Short RSS | Manager `/gather/shortrss` | [legacy RSS](../../server/models/modelShortRss.js), [RSS adapter](../../server/controllers/kma/kma.town.short.rss.controller.js); the parser now stores the English wind label in `wdEn` (`658605db`) | Used only when 0–24 h old; newer publication overrides / equal publication fills unusable fields in future slots, field by field; `ws`/`wd` map to `wsd`/`vec` ([RSS fallback](../architecture/mobile-api.md#rss-fallback-contract-issue-2554-local-repair)) |
| Medium forecast/land/temp/sea | Manager medium gather endpoints (`MidFcstInfoService` on `apis.data.go.kr` since `5e653285`). Land/temperature batches pass [`midForecastPolicy.parse`](../../server/lib/midForecastPolicy.js#L155-L187) (`95fe711e`): result code `00`, `totalCount` equal to the item count, a 06:00/18:00 KST publication, and per item at least one recognised weather text (land) or temperature (temperature); invalid horizons stay absent | [medium adapter](../../server/controllers/kma/kma.town.mid.controller.js), [legacy forecast](../../server/models/modelMidForecast.js), [land](../../server/models/modelMidLand.js) (adds `rnSt3Am`…`rnSt10`), [temperature](../../server/models/modelMidTemp.js) (no `-100` default since `95fe711e`) | Forecast point, land region and temperature city/zone have different keys. Land and temperature each count only when 0–36 h old and self-consistent ([item 16](server-response-assembly.md#3-domestic-merge-rules-in-execution-order)). Full mobile assembly consumes forecast/land/temp; collection of sea data does not mean this route returns it. |
| Medium RSS (retired, `95fe711e`) | None: [`rssEnabled = false`](../../server/lib/midForecastPolicy.js#L14) gates the requester's entry points (`start`, `mainProcess`, `mainProcessM`, `processGetMidRss`, `checkGetTime`) and both save functions; the Manager no longer queues `midrss` (`49afbbea`), and a manual `GET /gather/midrss` logs the retirement error and returns an empty 200 | [requester](../../server/lib/midRssKmaRequester.js), [legacy model](../../server/models/modelMidRss.js), [v2 model](../../server/models/kma/kma.town.mid.rss.model.js) | Not read: `getMidRss` is a pass-through. Existing documents remain in Mongo without a reader ([weather collection](../architecture/weather-collection.md#daily-forecast-validity-issue-2560)) |
| Station hourly/minute | Scraper jobs | [station controller](../../server/controllers/controllerKmaStnWeather.js), [hourly v2](../../server/models/modelKmaStnHourly2.js), [minute v2](../../server/models/modelKmaStnMinute2.js), older models also imported by controller | Nearest station, station timestamp, observed rain/cloud/temperature; repairs grid history/current ([station selection](server-response-assembly.md#station-selection-and-merge)) |
| Historical ASOS observations (opt-in, `2116c6bf`) | Queued `AsosHistory` Manager job at UTC minute 2 and startup `putAll` (gather/local) when [`ASOS_HISTORY_ENABLED=true`](../../server/config/config.js#L10-L15) with `ASOS_HISTORY_SERVICE_KEY` and an `ASOS_HISTORY_STATIONS` list; or the operator command [`backfill-history.js`](../../server/bin/backfill-history.js) `--station ID --start YYYYMMDD --end YYYYMMDD` (at most 7 completed KST dates; no app startup). Both fetch KMA ASOS hourly/daily records for missing D−7..D−1 keys only ([recovery](../../server/lib/history/recovery.js), [provider](../../server/lib/history/provider.js)) | Native collections `asos_history` (`_id` `<hourly or daily>:<station>:<KST key>`, insert-only per field) and `asos_history_leases` (15-minute owner lease per station), independent of `DB_DATA_VERSION` ([store](../../server/lib/history/store.js)); no TTL or pruning | Read only when `ASOS_HISTORY_READ_ENABLED=true`: nearest configured city station within 100 km fills invalid hourly history and daily fields and yields `historyStatus` ([items 10 and 20](server-response-assembly.md#3-domestic-merge-rules-in-execution-order)). Operator contract and schedule: [historical ASOS recovery](../architecture/weather-collection.md#historical-asos-recovery-2564), [diagram](../architecture/diagrams/historical-observations.html) |
| Weather warnings | Scraper special-weather job; `announcement` is parsed in the scraper process's local time zone | [special controller](../../server/controllers/kma.specialweather.controller.js), [situation model](../../server/models/modelKmaSpecialWeatherSituation.js) | Latest document per type. Town/near-station filtering of type 1 only for `current.specialInfo`; all four types, with a −9 h announcement shift and a 10 h flash expiry, for `/kma/special` ([rules](server-response-assembly.md#8-weather-warnings)) |
| Life indices | Scheduled life-index requester | [life controller](../../server/controllers/lifeIndexKmaController.js), [v2 model](../../server/models/kma/kma.lifeindex.model.js), [legacy model](../../server/models/lifeIndexKma.js) | Area number/geography and date; daily indices |
| Health indices | Scheduled health-day controller | [health model](../../server/models/modelHealthDay.js) | Area number, date, nearest-area fallback |
| AirKorea station/regional observations | Scheduled `keco` / `kecoSido` | [air controller](../../server/controllers/kecoController.js), [station readings](../../server/models/arpltnKeco.js), [region readings](../../server/models/sido.arpltn.keco.model.js), [station metadata](../../server/models/modelMsrStnInfo.js) | Town→station(s), measurement time; latest air + history + air standards ([station selection](server-response-assembly.md#station-selection-and-merge)) |
| Regional daily air forecast | `/gather/kecoForecast` | [forecast model](../../server/models/modelMinuDustFrcst.js) | Region + date; daily pollutant forecasts |
| Hourly air forecast | KAQ/AirKorea image collection and decoding ([image-derived hourly air forecast](external-providers.md#image-derived-hourly-air-forecast)). App weather URLs always carry `airForecastSource=kaq` ([`_getUnitsParams`](../../client/www/js/service.weatherutil.js#L78-L89)); the server default for requests without it is `airkorea` | [KAQ controller](../../server/controllers/kaq.hourly.forecast.controller.js), [hourly KAQ](../../server/models/kaq.hourly.forecast.model.js), [map case](../../server/models/kaq.map.case.model.js), [AirKorea controller](../../server/controllers/airkorea.hourly.forecast.controller.js), [hourly AirKorea](../../server/models/arpltn.hourly.forecast.js) | Station forecast and publication; query `airForecastSource` chooses source |
| Sunrise/sunset | Scheduled KASI collection | [KASI controller](../../server/controllers/kasi.riseset.controller.js), [rise/set model](../../server/models/modelKasiRiseSet.js) | Nearest geographic location/date; enrich daily weather |
| World (DSF-named; Visual Crossing since #2585) | Request-time fill in `getDsfData`: one Timeline call under a per-location `vc.fetch.locks` lock | [DSF cache controller](../../server/controllers/worldWeather/dsf.controller.js), [Visual Crossing requester](../../server/lib/VC/vcRequester.js), [DSF model](../../server/models/worldWeather/dsf.model.js), [lock model](../../server/models/worldWeather/vc.fetch.lock.model.js), [usage model](../../server/models/worldWeather/vc.usage.model.js) | Exact `[lon,lat]` + `dateObj`, local-day selection and current freshness; daily call counters in `vc.usage` |
| World air (WAQI) | Request-time station/feed or geographic lookup | [AQI controller](../../server/controllers/worldWeather/controllerAqi.js), [AQI model](../../server/models/worldWeather/modelAqi.js) | Geography/feed ID/measurement time; independently aged air enrichment |
| Time zone | Zone/coordinate cache and external resolution | [time-zone controller](../../server/controllers/timezone.controller.js) | Offset minutes; world local-day/history and DST-sensitive alignment |

Legacy KMA per-grid documents contain an array (`currentData`, `shortData`, etc.) and `pubDate` as `YYYYMMDDHHMM`. V2 current/short families use separate valid-time records with `mCoord`, `fcsDate`, `pubDate` Date and a weather payload. Their adapters project back to the older `{pubDate, ret}` request context; since `49afbbea` the short adapters add `dailyRows` (DB 1.0 from `dailySource`, DB 2.0 from each stored slot). Medium products have their own schema and publication types; they are not covered by a single current/short migration. The ASOS history store is a third, separate format keyed by explicit KST strings with UTC `date` values; it does not use either grid format.

The world cache stores UTC observation dates, an offset in minutes, current/hourly/daily data and `address`. In the DSF mapping, `address.country` holds the provider time-zone identifier (Visual Crossing `timezone` since #2585): it must not be interpreted as an ISO country code. Since #2585 the cache also uses it through `Intl` to find the offset in effect now for today/yesterday selection. `geo` is `[longitude, latitude]`; the client uses `{lat, long}`, the world JSON location uses `{lat, lon}`, and several intermediate helpers use `{lat,lon}`.

## Process modes and startup dependencies

`SERVER_MODE` defaults to `local`; `DB_DATA_VERSION` defaults to `1.0`; `ASOS_HISTORY_ENABLED` and `ASOS_HISTORY_READ_ENABLED` are off unless exactly `true`, and `ASOS_HISTORY_STATIONS` defaults to an empty list ([history config](../../server/config/config.js#L10-L15), `2116c6bf`). [Configuration](../../server/config/config.js), [startup](../../server/app.js).

| Mode | Automatic work established by `app.js` | Meaning for rewrite |
| --- | --- | --- |
| `service` | HTTP routes and Manager construction; no `startManager`/`startScrape` or push loops | Request-time Visual Crossing/AQI fills and unknown-town inserts still occur; mode is not a guarantee of read-only process behavior |
| `gather` | `startManager` | Metadata/key setup, immediate collection pass, timer, DSF/time-zone maintenance; the ASOS recovery job only when `ASOS_HISTORY_ENABLED=true` |
| `scrape` | `startScrape` | Station minute/hourly and special-weather scraping |
| `local` | Both gather and scrape startup | Running the app can immediately call providers and write data; two `task()` loops share the same task array |
| `push` | Push controller and alert-push loops; the APNs feedback call was removed in `45b2eb3f` | Scheduled alarm/special-alert dependencies are separate from weather read APIs ([push notifications](../architecture/push-notifications.md)) |

Routes are mounted independently of mode. `/gather/*` GET endpoints remain visible and can write data; some require objects initialized only by gather startup. A listener or `/health` response does not prove collector readiness or product freshness. A rewrite should make role ownership, initialization and route authorization explicit without assuming mode currently provides access control.

## Scheduled domestic ingestion

1. `startManager` initializes forecast-zone, air-station/key and life-index state, starts DSF/time-zone maintenance, then performs `checkTimeAndRequestTask(true)` and checks due tasks every 60 seconds.
2. `_requestApi` sends self-HTTP to configured `/gather/<name>` with a 24-hour request timeout. The gather route invokes the product collector; this timeout is distinct from each provider HTTP timeout.
3. KMA forecast collection determines publication time/grid, calls `collectTownForecast.requestData`, parses XML/category rows and normalizes product fields. Since `5e653285`/`b4d063fd` the seven product URLs are `http://apis.data.go.kr/1360000/...` ([L145-L151](../../server/lib/collectTownForecast.js#L145-L151)); a response counts only with result code `00`, a positive `totalCount` equal to the items merged from its 999-row pages (bounded sequential pagination since #2590), and a non-empty organized batch without non-finite values ([`getData`](../../server/lib/collectTownForecast.js#L346-L516), [`_emitOrganizedData`](../../server/lib/collectTownForecast.js#L565-L588)). `PCP`/`SNO`/`TMP` map into `r06`/`s06`/`t3h` without period conversion (hourly values; since #2583 a precipitation category keeps a representative amount and its text in `r06Text`/`s06Text`/`rn1Text`), and diagnostics no longer log key-bearing URLs ([reconciliation](../architecture/gather-source-reconciliation.md#compatibility-implemented)). Provider requests use a 10-second timeout; requester retry policy and Manager recursion are separate layers.
4. `_recursiveRequestData` starts with budget 70 for whole-grid current/short/shortest work. Completed results save serially; failed or invalid coordinates can be recursively retried (zero-delay rescheduling, not exponential backoff). Some invalid-temperature retry paths adjust publication time.
5. `getSaveFunc` dispatches to a product writer. Current/short/shortest writer wrappers recognize DB versions `1.0` and `2.0`; an unknown value has no generic successful save branch. Product cleanup and publication checks vary by product. No transaction covers weather, station, air, life and astronomy together.

Sources: [Manager](../../server/controllers/controllerManager.js) (`startManager`, `_requestApi`, `_recursiveRequestData`, `getSaveFunc`, `saveShort`), [gather routes](../../server/routes/v000001/routeGather.js), [KMA requester](../../server/lib/collectTownForecast.js), [mid forecast policy](../../server/lib/midForecastPolicy.js), [history service](../../server/lib/history/service.js).

### Trigger matrix

Times below are literal scheduler checks, **not provider publication guarantees**. Minutes are UTC (the minute value happens to match Korea's whole-hour offset).

| Task | Check | Dispatch |
| --- | --- | --- |
| Current | Minutes 2,12,22,32,42,52 | Direct self-HTTP |
| Shortest | 48,54,4,14 | Direct self-HTTP |
| Short | 13 | Direct self-HTTP |
| Station air / regional air | 3,13,23,33,43,53 / 4,14,24,34,44,54 | Direct self-HTTP |
| ASOS history recovery, only with `ASOS_HISTORY_ENABLED=true` (`2116c6bf`) | 2 | Pushed first in the minute-2 group, so it runs last in that LIFO batch; two in-process workers, stations admitted for 5 minutes, then resumed on the next run ([`scheduled`](../../server/lib/history/service.js#L79-L124)) |
| Past, dust forecast, medium temp/land/forecast/sea, short RSS | 2 | Queued. `midrss` was removed in `49afbbea` ([minute-2 group](../../server/controllers/controllerManager.js#L2119-L2165)) |
| Life | 10 | Queued |
| Health | 10 and `getUTCHours()+9 === 6 || === 18` | Queued; no modulo 24 in this expression |
| KAQ hourly | 7 at UTC hours 8,9,10,11,20,21,22,13 | Queued controller call |
| Station rain hit-rate | 50 | Direct, not forced by startup `putAll`. It reads the legacy `KmaStnMinute` collection, which has no live writer, and the v000901–v000903 chains do not read its result ([station selection](server-response-assembly.md#station-selection-and-merge)) |
| KASI rise/set | 55 | Direct self-HTTP |
| Scrape station minute | Even minutes | Queued by `startScrape` timer |
| Scrape warnings | Minutes divisible by 3 | Queued |
| Scrape station hourly | 4,6,9,15 | Queued |

Startup `putAll` forces Manager tasks other than rain hit-rate, including the ASOS job when enabled. The health expression cannot reach 6 because adding nine yields 9–32; the KAQ hour list ends in literal `13`. These are observed source quirks, not corrected intentions or proven current production schedules.

`task()` pops the pending array into a batch and runs `async.series`: **LIFO batches**, not FIFO delivery. The next drain is scheduled 30 seconds after completion. Direct self-HTTP jobs bypass this series and can overlap; multiple processes have no visible shared scheduler lock in this path. A pending array longer than 17 causes a logged process exit; the enabled ASOS job adds one queued task, and the removed `midrss` task one fewer. Durable queueing, leader election, idempotency keys and rate limits are rewrite design decisions, not capabilities demonstrated by this code.

## Freshness, fallback and failure boundaries

| Boundary | Observed behavior | Required rewrite decision / test |
| --- | --- | --- |
| Domestic weather read | Primarily DB reads; per-product publication dates survive assembly; read errors often log and continue | Define required versus optional products, maximum age and explicit partial response metadata |
| Unknown domestic town | Kakao → Google geocoding at request time; an in-Korea hit is written to `towns` without awaiting the save; a miss is not cached, so items 4–7, 10, 11, 14 and 24 repeat the geocoding and each continues. The `ff7acf39` crash-class dereference at item 10 was resolved upstream in `2116c6bf` (source reading, not reproduced; [location resolution](server-response-assembly.md#location-resolution-ordered)) | Decide whether reads may create reference data; cache negative lookups; test unknown, non-Korean and geocoder-failure towns with call counts and write counts |
| Current grid observation | Exact hour, then observation under three hours old, then forecast-derived current if a stale record was returned | Distinguish stale, unavailable, estimated and observed values |
| Station correction | Hourly history repairs invalid temperature; minute/station fields merge with per-field validity and timestamp rules. Station choice, rain consensus and the AirKorea donor merge with its cumulative freshness window: [station selection](server-response-assembly.md#station-selection-and-merge) | Characterize overlapping and disagreeing stations, valid zero, missing and sentinel values |
| KASI read | `getRiseSetList` returns DB results even when empty because `rsListFromDB.length >= 0`; the following API fallback is unreachable on normal array results | Do not promise on-demand sunrise recovery because a requester function exists. Decide whether to activate fallback in a new contract. |
| Nationwide overview | Regional air read followed by 15 parallel configured API-server HTTP weather requests, each with 9s timeout. The deployed `API_SERVER` (2026-09-20) was the public CloudFront host, so children re-enter the edge; the first child error fails the whole response ([§6](server-response-assembly.md#6-nationwide-screens-regional-air-plus-http-fan-out), [diagram](diagrams/server-nation-fanout.html)) | Define aggregate concurrency budget, partial-city policy and retry amplification; decide between edge self-calls and in-process composition |
| KMA preload | Independent family groups and later re-reads; no coherent all-product snapshot | Freeze clocks/publications in comparison fixtures; do not infer atomic freshness from request completion |
| Short and short RSS age (`b13dc38a`, `49afbbea`) | A short or short RSS publication counts only when 0–24 h old by the request clock; a future timestamp is rejected. A stale primary short still fills the hourly grid, but its daily overlay is replaced by RSS-contributed fields or dropped | Decide whether stale hourly data should also be withheld or flagged; freeze the request clock in tests (upstream froze it in `2f46bf15`) |
| Medium daily validity (`95fe711e`) | Land and temperature each need a 06:00/18:00 KST publication 0–36 h old matching the stored `pubDate`; otherwise that source contributes no days. Only complete rows in D−7..D+10 remain, and `midData.dailyStatus` lists reasons and `unavailableDates` ([daily validity](../architecture/mobile-api.md#daily-forecast-validity-issue-2560)) | Keep the health metadata or an equivalent; test stale, mismatched, partial-horizon and shower-label publications |
| ASOS history read (`2116c6bf`) | Opt-in; 3 s bound; station, cache or timeout failures become `historyStatus.reason`; fills only invalid fields; never calls the provider | Treat recovered observations as a separate provenance; test enabled/disabled, missing stations and partial days |
| ASOS recovery job | Opt-in; 10 s per HTTP attempt and up to three attempts for network, timeout, 429 or 5xx failures, at most eight 999-row pages; per station a 10-minute work deadline and a 15-minute lease; completion from stored readback. November–March hourly rain is not stored as `rn1` ([weather collection](../architecture/weather-collection.md#historical-asos-recovery-2564)) | Assign job ownership and alerting; the scheduled report is only logged |
| DSF-named world cache | 15-minute current check plus local-day selection; any missing yesterday/today/current record triggers one Visual Crossing call (`combined` when yesterday is missing, else `forecast`), coordinated across workers by a 10-second Mongo lock; waiters poll 250 ms for up to 2.5 s. Reads cover the last 3 days. The response waits at most 2.5 s while the lock holder's call may run to 8 s and still store its records. Provider calls stop for 2 s per location after a failure, for 10 min after a daily-limit 429 or rejected key, and whenever a call would exceed the optional `VC_DAILY_RECORD_LIMIT` for the UTC day | Model cache key, source, valid time, fetch time and local date separately |
| DSF-named yesterday | Since #2585 a stored yesterday is used as is: no hourly-completeness check, no hole filling and no history refetch; it is fetched only when missing, inside the single `combined` call | Test midnight, DST days, missing one hour and missing yesterday entirely |
| DSF-named current/time zone | Zone from Visual Crossing `timezone`; the offset at request time from the hour rows (the top-level `tzoffset` is the range-start offset); no Google lookup on this path. When no fresh data can be fetched, a stored current record (fresh, or up to 3 h old) is served with any stored yesterday and today; without one, the failure (provider or conversion error, lock-wait timeout, suppressed call) and any formatting exception are `Error` objects → `next(err)` → Express default HTML 500. The former string-valued failures and callback-time `TypeError` were removed in #2585 ([§5 step 3](server-response-assembly.md#5-world-weather-request-time-cache-fill-then-merge)) | Explicit time-zone fallback policy; avoid silently assigning wrong day; characterize each provider failure with a fake provider before replacing the error contract |
| DSF-named write | Save errors log and continue; current is saved last so lock waiters see a complete set | Current response availability does not establish durable history or cache consistency |
| AQI cache | 60-minute freshness; known-feed then geographic fallback; missing air often tolerated, propagated errors still fail parallel join | Test absence separately from callback error and corrupt records |
| AQI join | `_compareDate` requires equal `getUTCDate()` and `(weatherTime-airTime)/hours <= 6`, with **no lower bound** | Source quirk: future measurements may qualify; month/year equality is not explicitly checked. Decide and test intended temporal window. |
| Gather handler | Some routes log collection errors but still send an empty successful response | Measure product publication progression and ingestion failures, not just HTTP status |
| DSF maintenance | Daily removal of records older than two days starts with the gather Manager (`gather` or `local` mode), not service mode. `vc.fetch.locks` documents expire by TTL; `vc.usage` keeps one small document per UTC day and is never purged. WAQI records are pruned differently: per requested coordinate, during any world request, including in `service` mode ([retention table](data-model-reference.md#2-retention-and-pruning)) | Assign retention/cleanup ownership explicitly |

Sources: [domestic assembly](../../server/controllers/controllerTown.js), [mid forecast policy](../../server/lib/midForecastPolicy.js), [history](../../server/lib/history/), [KASI reader](../../server/controllers/kasi.riseset.controller.js), [DSF cache](../../server/controllers/worldWeather/dsf.controller.js), [AQI join and retrieval](../../server/controllers/worldWeather/controllerWorldWeather.js), [gather routes](../../server/routes/v000001/routeGather.js).

Since #2585 the active path uses the [Visual Crossing requester](../../server/lib/VC/vcRequester.js) (whole-call budget including one retry, 8 seconds as `DsfController` calls it; one retry only for a concurrency 429 within 1.5 seconds or a reset socket; gzip; `VC_SECRET_KEY`). The legacy [DSF requester](../../server/lib/DSF/dsfRequester.js) no longer targets Dark Sky, which was retired in March 2023; every method returns an error without a network call. Modules for older WU/other providers and a legacy `controllerCollector.doCollect` loop are not evidence that they run on the current v000903 path; the inspected app startup does not invoke that collector loop.

## Rewrite dependency inventory

| Dependency | What must be recovered or replaced | Evidence boundary |
| --- | --- | --- |
| Gateway | Public path/version mapping, country/geocode dispatch, headers, TTL/error behavior | Outside checkout; [2026-09-20 AWS/code evidence](../architecture/aws-code-correlation.md) is a timestamped starting point |
| Geographic data | Domestic town/grid mappings, address aliases, station lists, forecast zones, world time-zone lookup; request-time Kakao/Google geocoding that inserts unknown towns ([geocoder calls](external-providers.md#geocoding-and-coordinate-services), [reference-data provenance](data-model-reference.md#3-reference-data-provenance)) | DB/import content and current completeness not verified by source inspection |
| Weather providers | Raw fixtures, credentials, quotas, publication calendars, history availability, rain/snow/unit semantics ([provider catalog](external-providers.md)). At `bd6640f2` the KMA grid and medium products use `apis.data.go.kr`, whose hourly `PCP`/`SNO`/`TMP` fields are not period-equivalent to the stored six-hour/three-hour names ([period contract](../architecture/gather-source-reconciliation.md#period-contract-and-consumer-limitations)); the mid RSS feed is retired without replacement; ASOS history needs its own service key | Historical source URLs alone do not prove a usable provider; the upstream offline tests use synthetic fixtures, not live responses |
| Air data | Station identity, geographic coverage, index standard, daily/hourly projections and map images | Air standards cannot be treated as interchangeable display labels |
| KASI/life/health | Area matching, locality/date granularity, missingness and update schedule | Sparse products are currently tolerated in several paths |
| Persistence | Both DB formats plus the version-independent `asos_history`/`asos_history_leases` collections and the DB 1.0 short `dailySource` snapshot, [Date/string timestamp conversion](data-model-reference.md#4-time-representations), geographic index order, [retention](data-model-reference.md#2-retention-and-pruning) and duplicate handling ([identity keys](data-model-reference.md#1-model-and-collection-inventory)) | No live export or schema migration was executed |
| Localized summaries | Locale messages, severity ranking, warning precedence, unit-aware text | Shared domestic helpers also shape world responses |
| Native clients | Unversioned/v000901 routes, shared preference format, widgets, push tokens/settings; v000901/v000902 response shapes ([callers and variants](server-response-assembly.md#7-mounted-version-variants-and-callers)) | Current Angular source does not enumerate every shipped consumer |
| Operations | Gather/scrape/service/push role ownership, scheduler liveness, per-product age, error logs, request correlation; the opt-in ASOS job and `backfill-history.js`, and the `KMA daily forecast degraded` warning (at most one per minute per process). New Relic was removed in `c80ee014` and the server is prepared for Node 16 (`server/.nvmrc`) | Historical service host used a different revision; separate gather host internals remain unverified |

Before replacement, retain approved raw input fixtures and expected composed responses, each carrying source revision, clock/time zone, database version, units, locale and product publication dates. Keep live credentials out of fixtures and documentation. Design comparison tests around business semantics (extrema, rain totals, missingness, yesterday and station selection), not merely structural JSON equality. The [verification matrix](verification-matrix.md) and [rewrite playbook](rewrite-playbook.md) translate these dependencies into migration gates.

## Scope and limitations

This lane changed documentation and diagrams only. The 2026-09-25 re-baseline to `bd6640f2` re-read every statement that touches the upstream server diff; it did not run the upstream offline tests. Diagram artifact validation, browser containment and image review are recorded separately in [server diagram verification](../../reports/rewrite-verification/server-diagram-verification.md). No live database/provider check, server test suite, collection job or deployment was run. Existing integration suites include provider/database dependencies and cannot be reported as passed from static reading.

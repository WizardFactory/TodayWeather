# Server and client rewrite playbook

Source baseline: `bd6640f2` (re-baselined 2026-09-25 from `ff7acf3996ccb66c912d2ed4710cf300197d6966`, inspected on 2026-09-23; client and native source identical at both commits); reference links updated by the 2026-09-24 gap review, its 2026-09-25 final round and the [re-baseline](README.md#re-baselined-to-bd6640f2). This document proposes a migration process; it does not select a framework, database, weather provider or deployment platform. Existing behavior is described separately from proposed changes in the [decision register](decisions-and-open-questions.md). The [verification matrix](verification-matrix.md) defines follow-up checks, not tests already passed.

## What must survive the rewrite

The system is more than a weather endpoint and two forecast screens. It includes TodayWeather and TodayAir, country/weather-warning views, location and unit preferences, push settings and workers, purchase variants, native widget/watch consumers and, since `bd6640f2`, a static web PWA that calls four v000903 contracts from the browser ([native consumers §5](native-consumers-and-plugins.md#5-non-native-consumer-web-pwa-web)). The shared app requests v000903, while native consumers and deployed public defaults include older contracts. The [30-day traffic report](../../reports/aws/api-traffic-2026-09-22.md) observed 70.72% of product requests on unversioned weather coordinates; it did not identify every caller. The iOS widgets read that unversioned path; their field dependencies are in the [widget compatibility checklist](native-consumers-and-plugins.md#43-compatibility-checklist), and the external Android widget plugins remain an unverified caller candidate ([native consumers and plugins](native-consumers-and-plugins.md#1-consumer-inventory)). Retiring old routes requires release and traffic evidence, not source-version assumptions; the [API endpoint catalog](api-endpoint-catalog.md#retirement-and-compatibility-decisions-proposal) lists per-route consumers, traffic and retirement proposals.

| Boundary | Existing constraint to characterize | Source |
| --- | --- | --- |
| Public API and backend | Public `/weather/...` and `/geocode/...` paths depend on external Lambda/gateway behavior; Express mounts alone do not implement them | [Mobile API](../architecture/mobile-api.md), [gateway correlation](../architecture/aws-code-correlation.md) |
| Domestic composition | Current depends on short/shortest; icons precede unit conversion; descriptions and final summary follow conversion | [KMA router](../../server/routes/v000903/route.kma.v000903.js), `routerList` |
| Data identity | App coordinates are `{lat,long}`; Mongo geospatial arrays are `[longitude,latitude]`; products use different grid/station/region keys | [WeatherUtil](../../client/www/js/service.weatherutil.js), [Town controller](../../server/controllers/controllerTown.js), [DSF model](../../server/models/worldWeather/dsf.model.js) |
| Time and validity | Publication time, retrieval time, local day, yesterday and `0000`/`2400` are distinct; missing values use field-specific sentinels | [Town controller](../../server/controllers/controllerTown.js), [Town24h](../../server/controllers/controllerTown24h.js), [KMA time library](../../server/lib/kmaTimeLib.js) |
| Storage generations | `DB_DATA_VERSION` selects supported domestic read/write formats; default configuration and observed deployed configuration differ | [Config](../../server/config/config.js), [service-host evidence](../architecture/ec2-internals.md) |
| Client state and native sharing | LocalStorage, app preferences, legacy key migration and widget sharing form one migration surface | [Storage service](../../client/www/js/service.storage.js), [weather widget](../../tw.ios/widget/TodayViewController.m), [air widget](../../ta.ios/widget/TodayViewController.m) |
| Product/platform builds | Build tasks select different configuration, resources and purchase controller/plugin combinations | [Gulp tasks](../../client/gulpfile.js), [client package](../../client/package.json) |
| Side effects | Collection, scraping and push run independently of weather rendering; a successful settings response is not proof of delivery | [Server startup](../../server/app.js), [push flow](../architecture/push-notifications.md) |
| Older mounted contracts | Widgets use v000901 shapes (CamelCase icons, singular `airInfo`); push workers use v000902; legacy `/town` and `/ww` chains differ in icons, units and air fields | [Version variants](server-response-assembly.md#7-mounted-version-variants-and-callers), [version routing diagram](diagrams/server-version-routing.html), [chain differences](api-endpoint-catalog.md#chain-differences-for-legacy-contracts) |
| Native perimeter | Widgets, Apple Watch, external Android widget plugins and per-variant Cordova plugins consume responses or shared preferences outside `client/www` | [Native consumers and plugins](native-consumers-and-plugins.md) |
| Language | Server prose, geocode names, push text and UI strings choose their language on different surfaces with different defaults | [Localization inventory](localization-inventory.md), [language negotiation](client-data-contracts.md#language-negotiation) |
| Stored time encodings | KST strings, +09:00 instants, epoch-shifted Dates and local-constructor Dates coexist; some stored dates depend on the writer's process time zone | [Time representations](data-model-reference.md#4-time-representations) |
| Units and sentinels | Conversions generally round to one decimal; KMA Fahrenheit is floored while world Fahrenheit is not; sentinels other than the KMA temperature `-50` are converted like values, so `-1` mm reaches the app as `0` in | [Unit conversion](domain-glossary.md#8-unit-conversion), D42 in the [decision register](decisions-and-open-questions.md#compatibility-and-operational-decisions) |
| Station-derived current values | Current air can merge several AirKorea stations up to 24 h old; current rain uses a city station plus a two-station consensus; the hourly air forecast the app requests is decoded from KAQ images with calibration that is not in the repository | [Station selection and merge](server-response-assembly.md#station-selection-and-merge), [image-derived hourly air forecast](external-providers.md#image-derived-hourly-air-forecast) |
| Device classes | The iOS projects declare iPhone and iPad with landscape, while the web layout sizes itself once per view from `window.screen` and does not react to rotation | [Device classes and orientation](screen-specifications.md#device-classes-and-orientation) |
| Forecast completeness (since `bd6640f2`) | Medium sources count only at 06:00/18:00 KST and up to 36 h old; short RSS counts up to 24 h, and the primary short forecast up to 24 h for daily rows but without an age limit for the hourly series; incomplete daily rows are dropped, so dates can be missing; `midData.dailyStatus` and the opt-in `historyStatus` describe gaps without changing the HTTP status; the mid RSS feed is retired | [Assembly §3](server-response-assembly.md#3-domestic-merge-rules-in-execution-order), [daily forecast validity](../architecture/mobile-api.md#daily-forecast-validity-issue-2560), D43, D44, D51 |
| Historical recovery (since `bd6640f2`, opt-in) | An ASOS job and a read path fill invalid past observations from native `asos_history` collections; both default off, and the collections have no retention rule | [Data model §1.8](data-model-reference.md#18-historical-observation-store-native-collections), [historical observation composition](../architecture/mobile-api.md#historical-observation-composition-2564), D47 |
| Browser consumer (since `bd6640f2`) | The web PWA sends credential-free cross-origin `GET`s with a fixed `Accept-Language: ko`, rejects redirects and mismatched air units, and needs CORS on success and error responses | [Native consumers §5](native-consumers-and-plugins.md#5-non-native-consumer-web-pwa-web), [web client](../architecture/web-client.md), D50 |
| Server runtime (since `bd6640f2`) | The server pins Node 16.20.2 with a lockfile, has no New Relic agent and delivers push only through FCM/GCM with lazily loaded Firebase credentials; the inspected service host still runs older code | [Configuration §1.9](configuration-inventory.md#19-upstream-configuration-changes-since-ff7acf39), [push §1.1](server-push-and-purchase.md#11-provider-changes-since-ff7acf39), [EC2 internals](../architecture/ec2-internals.md) |

Preserve observable contracts until an explicit decision changes them. This does not require reproducing source anomalies indefinitely. Record intentional corrections, affected clients and regression cases in the decision register.

## Suggested sequence and completion gates

| Stage | Proposed work | Evidence required before advancing |
| --- | --- | --- |
| 0. Freeze evidence | Pin source, screen captures, fixture inputs, raw responses, normalized client objects and operation inventory | Each artifact identifies revision, input, product, environment and evidence type; absent native/live coverage is visible |
| 1. Characterize behavior | Add deterministic tests around old parsers, composition functions and state transitions; include partial data and known anomalies. The Node [probe scripts](../../reports/rewrite-verification/probes/) (storage migration, push and entry links, push text and purchase expiry, time zones, unit conversion, AirKorea station merge) and the upstream isolated suites in [`server/test/offline`](../../server/test/offline/README.md) are starting points: each probe record names its command, the blob hashes of the executed source files, its inputs and the expected values (re-run at `bd6640f2`) | Fixtures produce explicit expected values and field presence; clock, network, storage and native dependencies are controlled |
| 2. Isolate boundaries | Introduce testable adapters for provider access, persistence, geocoding, clock, localization and native bridges | The read path runs against fixtures without production DB access, provider egress or background collection |
| 3. Rebuild weather reads | Implement ordered domestic composition and world cache/fill behavior; adapt any new internal representation to supported public contracts | Old/new outputs agree on required semantics, or each difference maps to an approved decision; KMA and world paths both pass |
| 4. Rebuild client and migrate state | Implement screens against captured contracts; migrate cities, units, settings, paid state and shared preferences | Screenshot and interaction checks pass; fresh install, upgrade, restart, resume, missing data and denied location are covered |
| 5. Rebuild collection and side effects | Move collectors, cache fills, scheduling, push and purchase functions in separately reversible increments | Duplicate execution, retry, partial failure, idempotency and recovery are verified with isolated dependencies |
| 6. Replace gradually | Compare read-only shadow outputs, then route a defined cohort to the replacement; retain a compatible fallback | Release owners set and measure mismatch, freshness, error and latency gates; rollback is rehearsed with data compatibility intact |

Stages can overlap after their shared contracts are stable. Keep API, client presentation, storage migration and provider replacement separable enough that a mismatch has a traceable cause. Do not combine a new provider's semantics with a new UI and a new public response schema in one unmeasured cutover.

Shadowing is a proposed technique, not the current deployment model. Mirror reads only when the replacement cannot trigger uncontrolled provider fills or writes. Do not duplicate notification delivery, receipt validation side effects or collector writes without a dedicated isolation strategy.

## How to construct a useful comparison baseline

For each case, retain the request, requested units/language, clock/timezone, product publication times, raw product records, legacy assembled response, normalized client model and final screen. This allows a mismatch to be traced through provider input → server merge → wire representation → client parsing → presentation.

Compare numeric values with explicit, field-specific rounding rules; the legacy factors, rounding and sentinel outcomes are in [glossary §8](domain-glossary.md#8-unit-conversion). Compare absence separately from `null`, zero, empty collections and invalid sentinels. Do not ignore every timestamp or textual summary: those values carry freshness, day grouping and localization semantics. If a new schema is proposed, document how a compatibility adapter preserves old clients before changing the wire representation. Code values, sentinels, icon names, air breakpoints, derived indices and summary rules are tabulated in the [domain glossary](domain-glossary.md). Send an explicit canonical language on every prose-bearing request and choose one missing-language default; see the [localization rules](localization-inventory.md#6-rewrite-rules-proposal).

For storage migration, take identity keys, indexes, retention owners, reference-data seeding (`part.csv`/`base.csv`, `coordinate2xy`) and the process-time-zone caveat for stored dates from the [data model reference](data-model-reference.md); record the process `TZ` with every fixture run.

The simulator images in this documentation establish rendering with synthetic fixture data. They do not establish complete Cordova behavior, provider availability, live API compatibility, notification delivery or purchase handling. Keep raw API examples and screenshot fixtures labeled separately so a convenient display fixture does not become an accidental API specification.

## Build and test isolation

[Server startup](../../server/app.js) connects Mongo during import. [Configuration](../../server/config/config.js) defaults `SERVER_MODE` to `local`, which starts both gathering and scraping. Characterization tests should load isolated functions/adapters where practical. If an application instance is necessary, use an explicit non-collecting mode, isolated database and controlled outbound dependencies; `service` mode alone does not disable database access or all mounted routes. `/gather/*` must not be used as a health probe. Every server environment name, its placeholder behavior and its import-time readers are listed in the [configuration inventory](configuration-inventory.md#1-server-environment-names). When staging fake providers or credential names for characterization, use the [external provider catalog](external-providers.md) as the per-endpoint checklist.

The existing [Gulp tasks](../../client/gulpfile.js) reset platform state and overwrite configuration/resources and purchase controller selection. A proposed replacement build should make product/platform inputs explicit and write to isolated outputs. Preserve private release configuration outside source-controlled documentation. Reproducing a local web render is a different milestone from a signed native build or deployable release. Task-by-task inputs, environment names, external release files and variant oddities are in the [configuration inventory](configuration-inventory.md#3-gulp-product-build-and-release-variants).

## Operational evidence and rollback

Before rollout, obtain current gateway/geocoder code, deployed configuration, active client versions, provider constraints and database evidence. The [architecture evidence index](../architecture/evidence.md) and [EC2 access reference](../architecture/ec2-access.md) identify known boundaries; their AWS observations are timestamped and must be rechecked when operational decisions depend on them.

Proposed observability should correlate request, product source, publication time, cache outcome, merge/fallback stage and response result without logging raw tokens or private location payloads; current log contents and sinks are listed in the [security and privacy inventory](security-and-privacy-inventory.md#31-log-sinks-and-levels). Collection and push need separate correlation for scheduled work and external submission. Measure provider acceptance and actual device delivery separately.

Rollback design must identify which service version can read newly written records, whether client preference migrations are reversible, and how background jobs avoid duplicate execution during transition. No numerical SLA, acceptable mismatch rate, retention period or rollout percentage has been decided by this document.

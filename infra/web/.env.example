WEB_DOMAIN=weather.example.com
WEB_PUSH_ENABLED=false
# Set these only on the deployment host; never commit actual keys.
WEB_PUSH_PUBLIC_KEY=
WEB_PUSH_PRIVATE_KEY=
WEB_PUSH_SUBJECT=
WEB_SESSION_SECRET=
